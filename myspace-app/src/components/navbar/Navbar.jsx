import "./Navbar.css"; //importamos el csstion Navbar () {
    /*
const iconStyles = {
padding: "5px",


*/

import { Link } from "react-router-dom";
import RocketIcon from "@mui/icons-material/Rocket";

/*


y pero props nos muestra el objeto de dentro
  const links = props.links;/**/ 
  
function Navbar ({header, links}) {
    return (
   <nav>
    <div>
            
        <RocketIcon/>
        <h2> {header} </h2>
    </div>
    
   <div>

(


    sss
     ={link.id}>
    {link.text}
    </link>
            ))}
   </div>
   </nav>
);
    
export default Navbar;
